import React from 'react'

import {Row} from 'antd';

const Notification = (props) => {

    return (
        <Row id="notification">
            <b>New Vether</b> was distributed to all previous owners.
        </Row>
    )
}

export default Notification
